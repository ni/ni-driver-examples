<Window x:Class="NationalInstruments.Examples.ResetAllDevices.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:NationalInstruments.Examples.ResetAllDevices"
        Title="Reset All Devices" Height="600" Width="525" Icon="App.ico">
    <Window.Resources>
        <local:AliasConverter x:Key="AliasConverter"/>
    </Window.Resources>

    <Grid Name="mainGrid" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Expander Header="Operating Instructions" IsExpanded="True" HorizontalAlignment="Left"  Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,20">
            <TextBlock xml:space="preserve" TextWrapping="Wrap" Padding="10,4,0,0" HorizontalAlignment="Left"><Bold>Overview:</Bold> Resets all hardware devices on the target specified.
<Bold>Requirements:</Bold> NI System Configuration, applicable device drivers
<Bold>Instructions:</Bold>
1. Enter the <Bold>Target</Bold> that contains the devices. Enter <Bold>User Name</Bold> and <Bold>Password</Bold> if necessary.
2. Click the <Bold>Reset Devices</Bold> button to discover and reset all supported devices on the target.</TextBlock>
        </Expander>

        <Grid Grid.Row="1" Margin="5" IsEnabled="{Binding GridEnabled}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Target"/>
            <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding Target, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" TabIndex="0"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="User Name" Margin="4,0"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Username, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" Margin="4,0" TabIndex="1"/>
            <TextBlock Grid.Row="0" Grid.Column="2" Text="Password" Margin="8,0,0,0"/>
            <PasswordBox Name="PasswordBox" Grid.Row="1" Grid.Column="2" Margin="8,0,0,0" TabIndex="2"/>
        </Grid>

        <Button Grid.Row="2" Margin="6" Height="30" IsDefault="True" Click="OnResetDevicesClick" VerticalAlignment="Top" TabIndex="3" Width="120" IsEnabled="{Binding GridEnabled}">Reset Devices</Button>

        <DataGrid Name="DeviceList" Margin="5" Grid.Row="3" ItemsSource="{Binding DeviceList}" AutoGenerateColumns="False" IsReadOnly="True" IsEnabled="{Binding GridEnabled}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Alias/Resource Name" Width="*">
                    <DataGridTextColumn.Binding>
                        <MultiBinding Converter="{StaticResource AliasConverter}">
                            <Binding Path="UserAlias"/>
                            <Binding Path="ResourceName"/>
                        </MultiBinding>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Product Name" Width="*" Binding="{Binding ProductName}"/>
                <DataGridTextColumn Header="Pass/Fail" Width="*" Binding="{Binding ResultString}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
